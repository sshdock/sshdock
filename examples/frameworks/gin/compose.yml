services:
  web:
    build:
      context: .
    ports:
      - "127.0.0.1:18103:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/ping"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
